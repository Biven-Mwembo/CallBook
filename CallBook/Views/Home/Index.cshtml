@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <a asp-action="Add" class="btn btn-primary">Add New Contact</a>

    <div class="contact-list">
   @*      @foreach (var contact in Model)
        {
            <a asp-action="Details" asp-route-id="@contact.Id" class="list-group-item list-group-item-action">
                <div class="d-flex w-100 justify-content-between">
                    <h5 class="mb-1">@contact.Name</h5>
                    <small>@contact.PhoneNumber</small>
                </div>
                <p class="mb-1">@contact.EmailAddress</p>
            </a>
        } *@
    </div>

    <a asp-action="Add" class="btn btn-danger fab">
        <i class="fas fa-plus"></i>
    </a>
</div>
